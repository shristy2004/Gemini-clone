services:
  - type: web
    name: aichatbot-backend
    runtime: java
    rootDir: backend
    plan: free
    buildCommand: chmod +x mvnw && ./mvnw clean package -DskipTests
    startCommand: java -jar target/aichatbot-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 21